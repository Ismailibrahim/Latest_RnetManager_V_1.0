# Application Configuration
NEXT_PUBLIC_APP_NAME=${NEXT_PUBLIC_APP_NAME:-RentApplicaiton}
NEXT_PUBLIC_APP_ENV=${NEXT_PUBLIC_APP_ENV:-development}

# API Configuration
# For production behind reverse proxy, use relative path:
# NEXT_PUBLIC_API_URL=/api/v1
# For development or direct API access, use full URL:
NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8000/api/v1}

# Currency Configuration
NEXT_PUBLIC_PRIMARY_CURRENCY=${NEXT_PUBLIC_PRIMARY_CURRENCY:-MVR}
NEXT_PUBLIC_SECONDARY_CURRENCY=${NEXT_PUBLIC_SECONDARY_CURRENCY:-USD}

# Node Environment (automatically set by Next.js, but can be overridden)
# NODE_ENV=production
