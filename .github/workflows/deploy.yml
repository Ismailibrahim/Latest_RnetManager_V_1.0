name: 🚀 Deploy to VPS via SSH

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      APP_DIRECTORY: /var/www/webapp
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_HOST: ${{ secrets.SSH_HOST }}
      APP_URL: ${{ secrets.APP_URL }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🔍 Verify SSH connection
        run: |
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 "${{ env.SSH_USER }}@${{ env.SSH_HOST }}" "echo '✅ SSH connection successful'"

      - name: 📋 Pre-deployment checks
        continue-on-error: true
        run: |
          ssh -o StrictHostKeyChecking=no "${{ env.SSH_USER }}@${{ env.SSH_HOST }}" <<'EOF'
          set +e  # Don't exit on error
          APP_DIR="/var/www/webapp"

          echo "🔍 Running pre-deployment checks..."

          if [ ! -d "$APP_DIR" ]; then
            echo "❌ Error: App directory not found: $APP_DIR"
            exit 1
          fi

          # Basic checks only (skip script execution to avoid permission issues)
          AVAILABLE=$(df "$APP_DIR" | tail -1 | awk '{print $4}')
          if [ "$AVAILABLE" -lt 1048576 ]; then
            echo "⚠️  Warning: Low disk space (less than 1GB free)"
          else
            echo "✅ Disk space check passed: $(($AVAILABLE / 1024 / 1024))GB free"
          fi

          if [ ! -f "$APP_DIR/deploy.sh" ]; then
            echo "⚠️  Warning: deploy.sh not found, will be copied from config/deploy/deploy.sh"
          else
            echo "✅ deploy.sh found"
          fi

          echo "✅ Basic pre-deployment checks completed"
          EOF

      - name: 📦 Copy code to server
        run: |
          rsync -avz --delete \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.next' \
            --exclude='backend/vendor' \
            --exclude='backend/storage/logs' \
            --exclude='backups' \
            -e "ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -o ServerAliveCountMax=20" \
            ./ "${{ env.SSH_USER }}@${{ env.SSH_HOST }}:/var/www/webapp/"

      - name: 🚀 Deploy to VPS
        timeout-minutes: 30
        run: |
          ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -o ServerAliveCountMax=20 -o TCPKeepAlive=yes "${{ env.SSH_USER }}@${{ env.SSH_HOST }}" <<'EOF'
          set -e
          APP_DIR="/var/www/webapp"

          echo "📂 Navigating to $APP_DIR"
          cd "$APP_DIR" || exit 1

          # CRITICAL: Verify deploy.sh has the latest backup-disable logic
          # This MUST be present or backup will run and cause SSH timeout
          echo "🔍 Verifying deploy.sh has latest backup-disable logic..."
          
          # Check for the critical backup disable flag
          if ! grep -q "DISABLE_BACKUP_FOR_AUTOMATED_DEPLOYMENT" "$APP_DIR/deploy.sh" 2>/dev/null; then
            echo "⚠️  WARNING: deploy.sh missing latest backup-disable logic"
            echo "This means rsync didn't update it or script is outdated"
            echo "Checking if script exists in expected location..."
            
            # Try to find the script
            if [ -f "$APP_DIR/deploy.sh" ]; then
              echo "Script exists but is outdated. This will cause backup to run!"
              echo "Attempting to wait for rsync to complete..."
              sleep 2
              # Check again
              if ! grep -q "DISABLE_BACKUP_FOR_AUTOMATED_DEPLOYMENT" "$APP_DIR/deploy.sh" 2>/dev/null; then
                echo "❌ ERROR: deploy.sh is still outdated after wait"
                echo "The script will create a backup which will cause SSH timeout!"
                echo "File: $APP_DIR/deploy.sh"
                echo "First few lines:"
                head -30 "$APP_DIR/deploy.sh" || true
                exit 1
              fi
            else
              echo "❌ ERROR: deploy.sh not found at $APP_DIR/deploy.sh"
              exit 1
            fi
          fi
          
          echo "✅ Verified deploy.sh has latest backup-disable logic (DISABLE_BACKUP_FOR_AUTOMATED_DEPLOYMENT)"

          echo "🚀 Running deployment script..."
          echo "ℹ️  Note: Code already synced from GitHub Actions, skipping git pull and backup"
          
          # Export environment variables and ensure they're available to the script
          export APP_DIRECTORY="$APP_DIR"
          export DEPLOY_ENV="production"
          export SKIP_GIT_PULL="true"
          export SKIP_BACKUP="true"
          
          echo "DEBUG: Before script - SKIP_BACKUP=$SKIP_BACKUP, SKIP_GIT_PULL=$SKIP_GIT_PULL"
          echo "DEBUG: Script location: $APP_DIR/deploy.sh"
          
          # Run deploy script with environment variables explicitly passed
          # CRITICAL: Pass variables both via export AND env command to ensure they're available
          SKIP_BACKUP="true" SKIP_GIT_PULL="true" APP_DIRECTORY="$APP_DIR" DEPLOY_ENV="production" bash "$APP_DIR/deploy.sh" || {
            echo "❌ Deployment script failed"
            exit 1
          }

          echo "✅ Deployment script completed"
          EOF

      - name: 🏥 Health check
        run: |
          ssh -o StrictHostKeyChecking=no "${{ env.SSH_USER }}@${{ env.SSH_HOST }}" <<'EOF'
          set -e
          APP_DIR="/var/www/webapp"
          APP_URL="${APP_URL:-}"
          if [ -z "$APP_URL" ]; then
            APP_URL="http://${SSH_HOST}"
          fi

          echo "🏥 Running health checks..."
          echo "Health check URL: $APP_URL/api/v1/health"
          sleep 5

          if command -v curl >/dev/null 2>&1; then
            HEALTH_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" "$APP_URL/api/v1/health" || echo "000")
            if [ "$HEALTH_RESPONSE" = "200" ]; then
              echo "✅ Backend health check passed (HTTP $HEALTH_RESPONSE)"
            else
              echo "⚠️  Backend health check returned HTTP $HEALTH_RESPONSE"
            fi
          else
            echo "⚠️  curl not available, skipping health check"
          fi

          if systemctl is-active --quiet nginx 2>/dev/null; then
            echo "✅ Nginx is running"
          else
            echo "⚠️  Nginx status unknown"
          fi

          if systemctl is-active --quiet php8.2-fpm 2>/dev/null || systemctl is-active --quiet php8.3-fpm 2>/dev/null; then
            echo "✅ PHP-FPM is running"
          else
            echo "⚠️  PHP-FPM status unknown"
          fi

          if command -v pm2 >/dev/null 2>&1; then
            if pm2 list | grep -q "rentapp-frontend"; then
              echo "✅ PM2 process (rentapp-frontend) is running"
            else
              echo "⚠️  PM2 process (rentapp-frontend) not found"
            fi
          else
            echo "⚠️  PM2 not installed or not in PATH"
          fi

          echo "✅ Health checks completed"
          EOF

      - name: ✅ Deployment status
        run: |
          echo "🎉 Deployment workflow completed!"
          APP_URL="${{ env.APP_URL }}"
          if [ -z "$APP_URL" ]; then
            APP_URL="http://${{ env.SSH_HOST }}"
          fi
          echo "Check your VPS: $APP_URL"
          echo "Health endpoint: $APP_URL/api/v1/health"
